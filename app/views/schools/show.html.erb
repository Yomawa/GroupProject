<h1>Schools#show</h1>
<p>Find me in app/views/schools/show.html.erb</p>
  <p><%= @school.name %></p>
  <p><%= @school.webpage %></p>
  <p><%= @school.logo %></p>
  <p><%= @school.address %></p>
  <p><%= @school.description %></p>
<%= link_to 'Edit', edit_school_path(@school), class:"btn btn-primary" %>
<%= link_to 'Delete', @school, method: :delete,data:{confirm: 'Are you sure you want to delete this school?'}, class:"btn btn-default" %>
